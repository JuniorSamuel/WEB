import { FiltrousuarioPipe } from './filtrousuario.pipe';

describe('FiltrousuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrousuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
